# === Rezervace frontend / production ===
# Varianta A (doporučeno): frontend i backend jsou na stejné doméně,
# backend je přesměrován přes /api reverzní proxy.
VITE_API_URL=/api

# Varianta B: API běží na samostatné subdoméně.
# VITE_API_URL=https://api.rezervace.example.cz

